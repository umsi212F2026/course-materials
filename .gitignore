node_modules/
.DS_Store

# Renders of each workflow's .bpmn. Untracked as of 2026-08-24: a render goes stale
# the moment the diagram is edited, and a stale picture is worse than none. Read the
# .bpmn in workflows/diagram/tools/bpmn-viewer.html, or re-render with render.mjs.
workflows/*/*.png

# Claude Code machine-local settings — absolute paths, never shared.
.claude/settings.local.json

# A practice quiz's draw, answers and verdicts while it is being graded. Transient by design:
# what lasts from a practice run is the attempt recorded in the topic's evidence, not the
# paperwork that produced it.
tmp/
