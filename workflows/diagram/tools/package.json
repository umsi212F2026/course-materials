{
  "name": "si212-diagram-tools",
  "version": "1.0.0",
  "private": true,
  "description": "BPMN validation and auto-layout for SI 212. Students install the dependencies; rendering and the offline viewer are instructor-only and live in devDependencies.",
  "type": "module",
  "scripts": {
    "postinstall": "node -e \"process.stdout.write('\\n  Students: npm install --omit=dev  (skips Puppeteer and its browser)\\n\\n')\""
  },
  "dependencies": {
    "bpmn-auto-layout": "^1.3.0",
    "bpmnlint": "^11.12.1"
  },
  "devDependencies": {
    "bpmn-js": "^18.6.2",
    "puppeteer": "^24.43.1"
  }
}
